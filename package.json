{
  "name": "starwake",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Two independent arcade shooters: STARWAKE ZENITH and HORIZON",
  "scripts": {
    "dev": "node tools/serve.mjs",
    "build": "node tools/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
